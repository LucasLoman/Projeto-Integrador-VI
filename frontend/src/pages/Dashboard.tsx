import {useEffect,useState} from 'react'; import {api} from '../api'; import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';
export default function Dashboard(){const [d,setD]=useState<any>(null);useEffect(()=>{api.get('/dashboard/').then(r=>setD(r.data))},[]);if(!d)return <p>Carregando...</p>;return <><h2>Dashboard</h2><div className="cards"><div className="card"><b>{d.products}</b><span>Produtos</span></div><div className="card"><b>{d.low_stock}</b><span>Estoque crítico</span></div><div className="card"><b>R$ {Number(d.inventory_value).toFixed(2)}</b><span>Valor em estoque</span></div><div className="card"><b>R$ {Number(d.sales_30d).toFixed(2)}</b><span>Vendas 30 dias</span></div></div><section><h3>Produtos mais vendidos (30 dias)</h3><div style={{height:300}}><ResponsiveContainer><BarChart data={d.top_products}><XAxis dataKey="product__name"/><YAxis/><Tooltip/><Bar dataKey="revenue"/></BarChart></ResponsiveContainer></div></section><section><h3>Sem venda nos últimos 90 dias</h3><table><thead><tr><th>SKU</th><th>Produto</th><th>Qtd.</th></tr></thead><tbody>{d.slow_products.map((p:any)=><tr key={p.id}><td>{p.sku}</td><td>{p.name}</td><td>{p.quantity}</td></tr>)}</tbody></table></section></>}
