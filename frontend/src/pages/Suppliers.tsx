import {FormEvent,useEffect,useState} from 'react';import {api} from '../api';
export default function Suppliers(){const [items,setItems]=useState<any[]>([]);const [name,setName]=useState('');const [lead,setLead]=useState(7);const load=()=>api.get('/suppliers/').then(r=>setItems(r.data));useEffect(load,[]);async function submit(e:FormEvent){e.preventDefault();await api.post('/suppliers/',{name,lead_time_days:lead});setName('');load()}return <><h2>Fornecedores</h2><form className="gridform" onSubmit={submit}><input placeholder="Nome" value={name} onChange={e=>setName(e.target.value)}/><input type="number" min="0" value={lead} onChange={e=>setLead(Number(e.target.value))}/><button>Cadastrar</button></form><table><thead><tr><th>Fornecedor</th><th>Prazo (dias)</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.name}</td><td>{i.lead_time_days}</td></tr>)}</tbody></table></>}
